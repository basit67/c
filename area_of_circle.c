#include <stdio.h>
int main()
{
    int r;
    float pi = 3.14;
    printf("what is radius of circle\n");
    scanf("%d",&r);
    printf("this is the area of cycle %f \n", pi*r*r);
    return 0;
}